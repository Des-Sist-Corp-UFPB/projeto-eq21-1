spring:
  config:
    import: optional:file:.env[.properties]
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/eq21
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

aws:
  s3:
    endpoint: http://localhost:9000
    public-endpoint: http://localhost:9000

logging:
  level:
    br.ufpb.dsc.mercado: DEBUG
    org.springframework.web: DEBUG
